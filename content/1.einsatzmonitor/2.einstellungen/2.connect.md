---
title: Connect-Verbindung
description: Anbindung des EinsatzMonitors an das Feuersoftware Connect Portal
---

# Connect-Verbindung

Die Verbindung zum Feuersoftware Connect Portal ist essentiell für den Empfang von Alarmen und anderen Einsatzdaten.

## Voraussetzungen

Bevor Sie die Connect-Verbindung herstellen können, benötigen Sie:

- [ ] Einen aktiven **Connect-Account**
- [ ] Eine gültige **Lizenz** für den EinsatzMonitor
- [ ] Die **Organisations-ID** Ihrer Organisation
- [ ] Ein **API-Token** aus dem Connect-Portal

## API-Token erstellen

![Connect EinsatzMonitor anlegen](/images/einsatzmonitor/Connect_Create_EinsatzMonitor.png)

1. Melden Sie sich im [Connect-Portal](https://connect.feuersoftware.com) an
2. Navigieren Sie zu **Einstellungen** → **API-Tokens**
3. Klicken Sie auf **Neues Token erstellen**
4. Vergeben Sie einen aussagekräftigen Namen (z.B. "EinsatzMonitor Wache 1")
5. Wählen Sie die erforderlichen **Berechtigungen**:
   - Alarme lesen
   - Verfügbarkeiten lesen
   - Kalender lesen
   - News lesen
6. Klicken Sie auf **Erstellen**
7. **Kopieren Sie das Token** - es wird nur einmal angezeigt!

::callout{type="warning"}
Das API-Token wird nur einmal angezeigt. Speichern Sie es sicher ab, bevor Sie das Fenster schließen.
::

## Verbindung im EinsatzMonitor einrichten

![Connect Verbindung aktiv](/images/einsatzmonitor/EinsatzMonitor_Connect_Aktiv.png)

### Schritt 1: Einstellungen öffnen

1. Starten Sie den EinsatzMonitor
2. Drücken Sie **F10** oder klicken Sie auf das **Zahnrad-Symbol**
3. Navigieren Sie zu **Connect**

### Schritt 2: Zugangsdaten eingeben

| Feld | Beschreibung |
|------|--------------|
| **Server** | `connect.feuersoftware.com` (Standard) |
| **Organisations-ID** | Ihre Organisations-ID aus Connect |
| **API-Token** | Das kopierte API-Token |

### Schritt 3: Verbindung testen

1. Klicken Sie auf **Verbindung testen**
2. Bei erfolgreicher Verbindung erscheint eine Bestätigung
3. Klicken Sie auf **Speichern**

## Verbindungsstatus

Der Verbindungsstatus wird in der Statusleiste angezeigt:

| Symbol | Bedeutung |
|--------|-----------|
| 🟢 **Grün** | Verbunden und aktiv |
| 🟡 **Gelb** | Verbindungsprobleme |
| 🔴 **Rot** | Keine Verbindung |

## Fehlerbehebung

### Verbindung fehlgeschlagen

::collapsible{title="API-Token ungültig"}
- Überprüfen Sie, ob das Token korrekt kopiert wurde
- Stellen Sie sicher, dass das Token nicht abgelaufen ist
- Erstellen Sie ggf. ein neues Token im Connect-Portal
::

::collapsible{title="Netzwerkfehler"}
- Prüfen Sie die Internetverbindung
- Stellen Sie sicher, dass die Firewall den Zugriff auf `connect.feuersoftware.com` erlaubt
- Prüfen Sie, ob Port 443 (HTTPS) offen ist
::

::collapsible{title="Keine Berechtigung"}
- Überprüfen Sie die Token-Berechtigungen im Connect-Portal
- Stellen Sie sicher, dass Ihr Benutzer die erforderlichen Rechte hat
::

### Verbindungsabbrüche

Bei häufigen Verbindungsabbrüchen:

1. Prüfen Sie die Stabilität Ihrer Internetverbindung
2. Verwenden Sie nach Möglichkeit eine LAN- statt WLAN-Verbindung
3. Kontaktieren Sie bei anhaltenden Problemen den Support

## Mehrere Monitore

Wenn Sie mehrere EinsatzMonitore betreiben:

- Erstellen Sie für jeden Monitor ein **separates API-Token**
- Dies ermöglicht eine bessere Nachverfolgung und Verwaltung
- Bei Problemen können einzelne Tokens deaktiviert werden

## Datensynchronisation

Nach erfolgreicher Verbindung werden automatisch synchronisiert:

- **Alarme** - In Echtzeit
- **Verfügbarkeiten** - Alle 30 Sekunden
- **Kalendereinträge** - Alle 5 Minuten
- **News** - Alle 10 Minuten

::callout{type="tip"}
Die Synchronisationsintervalle können in den erweiterten Einstellungen angepasst werden.
::

